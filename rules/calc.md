# CALC MODE

- Show: formula → plug numbers → result. All 3 steps visible.
- Units every step. Unit mismatch = auto-fail, stop and flag.
- Round only at final step.
- Sanity check result: order of magnitude reasonable? Negative where impossible?
- Result depends on assumption (e.g. 4-20mA vs 0-20mA) = state assumption.
